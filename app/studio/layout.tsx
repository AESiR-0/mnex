export default function StudioLayout({
  children,
}: {
  children: React.ReactNode
}) {
  // Studio layout - NextStudio handles its own HTML structure
  // This layout just passes through children
  return children
}
